import { BlogPostsAPI } from '../components/BlogpostsAPI';

export const BlogpostsAPI = (state = BlogPostsAPI, action) => {
    switch (action.type) {

        default:
          return state;
      }
};