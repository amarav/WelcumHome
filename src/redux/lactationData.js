import { TOPFOODS,INSTANTMILK,MUSTINCLUDES,GOODFOODS,VEGFOODS, NONVEGFOODS,SEEDS,FRUITS} from '../components/lactationData';


export const Topfoods = (state = TOPFOODS, action) => {
    switch (action.type) {
        default:
          return state;
      }
};
export const Instantfoods = (state = INSTANTMILK, action) => {
    switch (action.type) {
        default:
          return state;
      }
};

export const MustIncfoods = (state = MUSTINCLUDES, action) => {
    switch (action.type) {
        default:
          return state;
      }
};

export const Goodfoods = (state = GOODFOODS, action) => {
    switch (action.type) {
        default:
          return state;
      }
};

export const Vegfoods = (state = VEGFOODS, action) => {
    switch (action.type) {
        default:
          return state;
      }
};

export const Nonvegfoods = (state = NONVEGFOODS, action) => {
    switch (action.type) {
        default:
          return state;
      }
};

export const Seeds = (state = SEEDS, action) => {
    switch (action.type) {
        default:
          return state;
      }
};

export const Fruits = (state = FRUITS, action) => {
    switch (action.type) {
        default:
          return state;
      }
};